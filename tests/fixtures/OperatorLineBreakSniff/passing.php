<?php

declare(strict_types=1);

$isValid = $ready
    && $enabled;

$message = 'Hello, '
    . $name;

$config = [
    'label' => 'Some value'
        . ' continued on the next line',
];

$result = $first;

$flag = $count
    === $limit;

if (
    $count === 0
    || $count === 1
) {
    return true;
}
