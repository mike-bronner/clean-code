<?php

// The parser was hacked together in an afternoon and then rewritten properly.
# Built behind the shack, which is why the coordinates look odd.

/* XXXX is not a marker, it is the placeholder width of a masked card number */
/*
 * Hackathon notes: the queue driver was chosen in the first hour.
 */

/**
 * A report whose refactor did finish.
 *
 * TODO: owned by Generic.Commenting.Todo, so this sniff stays quiet on it.
 * FIXME: owned by Generic.Commenting.Fixme, likewise.
 *
 * @param string $hackathonSlug The event this report covers.
 */
class Passing
{
    // Unhackable is one word, and hacksaw is another.
    public function total(string $hackathonSlug): int
    {
        return strlen($hackathonSlug);
    }
}
