<?php

// HACK: resolve the container by hand until the binding lands.
// XXX the retry count is a guess
# HACK the flag is read straight off the request
# XXX

/* HACK - reaches for the facade because the service is not injectable yet */
/*
 * XXX
 */

/**
 * A report that never finished its refactor.
 *
 * HACK
 * XXX rounding drops a cent on every third row
 */
class Failing
{
    // HACK: this branch exists only for the legacy payload. XXX
    public function total(): int
    {
        return 1;
    }
}
