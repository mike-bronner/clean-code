<?php

// A trait, named so that only its token type keeps it out of the rule: the
// namespace names a different suite than the directory does, so registering
// T_TRAIT would report it.

namespace Tests\Feature;

trait BillingTest
{
}
