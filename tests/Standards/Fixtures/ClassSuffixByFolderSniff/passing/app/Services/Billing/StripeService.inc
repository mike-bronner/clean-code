<?php

declare(strict_types=1);

namespace App\Services\Billing;

// Nested below a generic base folder: the suffix still comes from the base
// folder (Services), not the nested one (Billing).
class StripeService
{
}
