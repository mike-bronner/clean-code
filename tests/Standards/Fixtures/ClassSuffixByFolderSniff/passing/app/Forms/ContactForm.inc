<?php

declare(strict_types=1);

namespace App\Forms;

// app/Forms derives the "Form" suffix itself, so a Form-suffixed class here is
// not a misplaced Livewire form — flagging it would leave the class unnameable.
class ContactForm
{
}
