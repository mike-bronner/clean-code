<?php

declare(strict_types=1);

namespace App\Http;

// Directly in app/Http: there is no segment below Http to derive a suffix
// from, so none is required — Laravel's own app/Http/Kernel.
class Kernel
{
}
