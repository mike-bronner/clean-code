<?php

declare(strict_types=1);

namespace App\Http\Controllers\Api;

// Nested below app/Http: the suffix comes from the segment directly under Http
// (Controllers), so the nested Api folder inherits the "Controller" suffix.
class TokenController
{
}
