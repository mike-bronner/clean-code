<?php

declare(strict_types=1);

namespace Acme\Services;

// No "app" segment in the path: the standard is scoped to a Laravel
// application folder, so the sniff must stay inert here.
class Payment
{
}
