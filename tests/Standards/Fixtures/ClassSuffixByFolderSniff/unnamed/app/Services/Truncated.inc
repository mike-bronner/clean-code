<?php

declare(strict_types=1);

namespace App\Services;

// A truncated declaration: the `class` keyword with no name token after it.
// PHPCS still tokenises it as T_CLASS, so the sniff must cope with a nameless
// class rather than crash while linting work-in-progress source.
class
