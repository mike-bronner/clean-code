<?php

declare(strict_types=1);

$isValid = $ready &&
    $enabled;

$message = 'Hello, ' .
    $name;

$config = [
    'label' => 'Some value' .
        ' continued',
];

$result =
    $first;

$flag = $count ===
    $limit;

$note = $a . // explains the wrap
    $b;
